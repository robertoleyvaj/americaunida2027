export default function DemoBanner() {
  return (
    <div className="bg-gold text-navy text-center text-xs md:text-sm font-medium px-4 py-2">
      Vista previa · Aún no se guardan datos. Esto se activa al conectar la base de datos (Supabase).
    </div>
  );
}
